@model CurrentUserViewModel
@{
	Layout = "";    
}
@if (Model.IsAuthenticated())
{ 
	<h1>欢迎 @Model.FullName</h1>
	<p><a href="@Url.Action("Logout", "Login")">注销</a></p>
}
else
{ 
	<h1>登陆</h1>
	<p><a href="@Url.Action("Index", "Login")">登陆</a></p>
}